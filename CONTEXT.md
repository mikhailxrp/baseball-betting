# Baseball Betting System — контекст проекта

## Что это

Рекомендательная система ставок на бейсбол MLB.
Агент анализирует матчи и рекомендует ставки на тоталы.

## Как работает

1. Получаем расписание матчей на день с MLB Stats API
2. Агент анализирует питчеров, составы, статистику команд
3. Система выдаёт рекомендацию: матч / тип ставки / линия / уверенность / причины
4. Пользователь делает ставку у букмекера вручную
5. Результат вводится в систему вручную
6. После накопления статистики включается финансовая система

## Тип ставок

- total_over — общий тотал больше
- total_under — общий тотал меньше
- ind_total_over — индивидуальный тотал команды больше

## Источники данных

- MLB Stats API (бесплатно, без ключа):
  https://statsapi.mlb.com/api/v1
- OpenWeather API (погода)

## База данных (Supabase / PostgreSQL)

Таблицы в порядке зависимостей:

1. teams — справочник команд
2. games — матчи
3. team_stats — статистика команд по сезонам
4. bets — ставки
5. bank — история банка

## Ключевая логика анализа

Тотал высокий когда:

- Питчер слабый (высокий ERA, FIP, WHIP)
- Команда атакующая (высокий R/G, OPS, wRC+)
- Высокий LOB в предыдущем матче серии (не реализовали преимущество)
- Мотивация команды высокая (борьба за плей-офф)

## Финансовая система

Включается когда команда набрала 10+ ставок и показывает стабильность.
Расчёт суммы догонялки:
следующая ставка = (проигрыш + желаемая прибыль) / (коэф - 1)

## Стек

- Next.js 16.2.1 (App Router, JavaScript)
- Supabase (PostgreSQL)
- Tailwind CSS
- n8n (оркестрация агента)
- Claude API (LLM анализ)
- shadcn

## UI — страницы и компоненты

### Структура страниц

- / (главная) → Расписание матчей
- /analysis → Результаты анализа
- /bets → Журнал ставок
- /bank → Банк и история

### Layout (src/app/layout.jsx)

- Sidebar слева — фиксированный, сворачивается
- Основная область справа — flex-1
- Sidebar цвет: #7371fc (medium-slate)
- Фон страницы: #ffffff

### Цветовая схема

- Primary: #7371fc (medium-slate)
- Secondary: #a594f9 (soft-periwinkle)
- Accent: #cdc1ff (periwinkle)
- Background: #ffffff
- Text: #1a1a2e

### Компоненты (src/components/)

- layout/Sidebar.jsx → навигация, сворачивается
- layout/Header.jsx → заголовок текущей страницы
- schedule/GameCard.jsx → карточка матча
- schedule/ScheduleGrid.jsx → сетка карточек
- analysis/AnalysisCard.jsx → результат анализа
- bets/BetsTable.jsx → таблица ставок
- bank/BankSummary.jsx → баланс и график

### Карточка матча (GameCard)

- Away @ Home жирным
- Время по Москве
- Питчеры (away и home) или TBD
- Badge серии (Матч X из Y)
- Кнопка "Анализировать"

### Правила UI

- Шрифт: Roboto
- Компоненты: только shadcn/ui + Tailwind
- Все тексты интерфейса на русском
- Названия команд и питчеров — оригинальные (английский)
- Мобильная версия не приоритет
